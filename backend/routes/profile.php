<!-- <?php
// session_start();

// if (isset($_SESSION['email'])) {
//     $userEmail = $_SESSION['email'];

// Ambil data user dari file JSON
//     $jsonData = file_get_contents('../frontend/static/users.json');
//     $users = json_decode($jsonData, true);

// Cari user berdasarkan email
//     $userData = null;
//     foreach ($users as $user) {
//         if ($user['email'] === $userEmail) {
//             $userData = $user;
//             break;
//         }
//     }

// Debugging: Pastikan data user ada
//     if ($userData) {
//         var_dump($userData); Cek apakah data user ditemukan
//         echo json_encode($userData); Mengirimkan data JSON
//     } else {
//         echo json_encode(['error' => 'User not found']);
//     }
// } else {
//     echo json_encode(['error' => 'User not logged in']);
// }
?> -->
