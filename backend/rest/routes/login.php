<!-- <?php
// session_start();  // Memulai session

// // Ambil data email dan password dari form login
// $email = $_POST['email'];
// $password = $_POST['password'];

// // Ambil data user dari file JSON
// $jsonData = file_get_contents('../frontend/static/users.json');
// $users = json_decode($jsonData, true);

// // Cari user berdasarkan email
// foreach ($users as $user) {
//     if ($user['email'] === $email && $user['password'] === $password) {
//         // Set session email jika login berhasil
//         $_SESSION['email'] = $user['email'];
//         header('Location: ../frontend/view/forms.html'); // Redirect ke halaman profile
//         exit;
//     }
// }

// // Jika login gagal
// echo 'Invalid login credentials';
?> -->
